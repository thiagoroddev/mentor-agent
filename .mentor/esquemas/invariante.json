{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Invariante",
  "type": "object",
  "required": [
    "id",
    "enunciado",
    "porque",
    "mecanismo",
    "declarada_em",
    "conferida_em"
  ],
  "properties": {
    "id": {
      "type": "string",
      "pattern": "^INV-\\d+$",
      "description": "Identificador unico da invariante (ex: INV-1, INV-2)."
    },
    "enunciado": {
      "type": "string",
      "description": "O que precisa continuar sendo verdade ou o que e proibido."
    },
    "porque": {
      "type": "string",
      "description": "Justificativa da invariante (motivo de negocio, seguranca ou arquitetura)."
    },
    "mecanismo": {
      "type": [
        "string",
        "null"
      ],
      "description": "O que automatiza a verificacao (ex: caminho do teste, tipo ou regra de lint). null se for regra manual sem teste."
    },
    "declarada_em": {
      "type": "string",
      "pattern": "^\\d{2}/\\d{2}/\\d{2}$"
    },
    "conferida_em": {
      "type": "string",
      "pattern": "^\\d{2}/\\d{2}/\\d{2}$"
    }
  },
  "additionalProperties": false
}
