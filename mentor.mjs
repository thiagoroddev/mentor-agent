#!/usr/bin/env node
// Atalho de linha de comando. Existe para nao precisar digitar o caminho dos scripts,
// e para fugir do `--` que o npm exige antes de repassar flags.
import './.mentor/scripts/cli.ts'
